import 'package:flutter/material.dart';

class KegelExercises extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text("Kegel Exercises"),
        )
    );
  }
}
